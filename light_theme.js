const fs = require("fs"); const path = require("path"); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith(".tsx") || file.endsWith(".ts")) results.push(file); } }); return results; } const files = walk("./src"); files.forEach(f => { let content = fs.readFileSync(f, "utf8"); content = content.replace(/bg-slate-900\/50/g, "bg-slate-50"); content = content.replace(/bg-slate-900\/40/g, "bg-slate-50/80"); content = content.replace(/bg-slate-900\/60/g, "bg-slate-50/80"); content = content.replace(/bg-slate-900/g, "bg-slate-50"); content = content.replace(/bg-slate-800\/50/g, "bg-white shadow-sm"); content = content.replace(/border-slate-800/g, "border-slate-200"); content = content.replace(/border-slate-700/g, "border-slate-200"); content = content.replace(/text-white\/70/g, "text-slate-600"); content = content.replace(/text-white\/60/g, "text-slate-500"); content = content.replace(/text-white\/50/g, "text-slate-400"); content = content.replace(/text-white\/40/g, "text-slate-400"); content = content.replace(/text-white\/30/g, "text-slate-300"); content = content.replace(/text-white\/20/g, "text-slate-300"); content = content.replace(/text-white/g, "text-slate-800"); content = content.replace(/text-zinc-300/g, "text-slate-600"); content = content.replace(/text-zinc-400/g, "text-slate-500"); content = content.replace(/text-zinc-500/g, "text-slate-400"); content = content.replace(/text-zinc-200/g, "text-slate-700"); content = content.replace(/stroke="#ffffff"/g, "stroke=\"#475569\""); fs.writeFileSync(f, content); }); console.log("Light Theme applied");
