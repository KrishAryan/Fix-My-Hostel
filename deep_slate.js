const fs = require("fs"); const path = require("path"); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith(".tsx") || file.endsWith(".ts")) results.push(file); } }); return results; } const files = walk("./src"); files.forEach(f => { let content = fs.readFileSync(f, "utf8"); content = content.replace(/text-slate-[4567]00/g, "text-slate-800"); content = content.replace(/stroke="#475569"/g, "stroke=\"#1e293b\""); content = content.replace(/text-zinc-[345]00/g, "text-slate-800"); content = content.replace(/Raise Ticket/g, "Raise Complaint"); fs.writeFileSync(f, content); }); console.log("Deep Slate applied");
