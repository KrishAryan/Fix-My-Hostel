const fs = require("fs"); const path = require("path"); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith(".tsx") || file.endsWith(".ts")) results.push(file); } }); return results; } const files = walk("./src"); files.forEach(f => { let content = fs.readFileSync(f, "utf8"); content = content.replace(/bg-black/g, "bg-slate-900"); content = content.replace(/border-white\/10/g, "border-slate-800"); content = content.replace(/border-white\/20/g, "border-slate-700"); content = content.replace(/bg-white\/5/g, "bg-slate-800/50"); content = content.replace(/text-white\/60/g, "text-zinc-400"); content = content.replace(/text-white\/50/g, "text-zinc-500"); content = content.replace(/text-white\/70/g, "text-zinc-300"); content = content.replace(/text-white\/80/g, "text-zinc-200"); fs.writeFileSync(f, content); }); console.log("Theme updated");
