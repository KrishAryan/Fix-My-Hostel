const fs = require("fs"); const path = require("path"); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith(".tsx") || file.endsWith(".ts")) results.push(file); } }); return results; } const files = walk("./src/app"); files.forEach(f => { let content = fs.readFileSync(f, "utf8"); content = content.replace(/border-slate-200/g, "border-slate-100"); content = content.replace(/text-slate-[34]00/g, "text-slate-500"); content = content.replace(/bg-blue-500\/20 text-blue-500/g, "bg-blue-100 text-blue-600"); content = content.replace(/bg-red-500\/20 text-red-500/g, "bg-red-100 text-red-600"); content = content.replace(/bg-emerald-500\/20 text-emerald-500/g, "bg-green-100 text-green-600"); content = content.replace(/bg-amber-500\/20 text-amber-500/g, "bg-yellow-100 text-yellow-600"); content = content.replace(/bg-primary\/20 text-primary/g, "bg-blue-100 text-blue-600"); content = content.replace(/hover:bg-white\/10/g, ""); content = content.replace(/bg-blue-600 text-slate-800/g, "bg-blue-600 text-white"); content = content.replace(/text-slate-800 text-sm/g, "text-slate-500 text-sm"); content = content.replace(/text-2xl font-bold/g, "text-2xl font-bold text-slate-800"); fs.writeFileSync(f, content); }); console.log("Style update applied");
